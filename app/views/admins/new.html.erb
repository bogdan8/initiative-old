<h2><%= t('views.admins.new.title') %></h2>
<%= form_for :user, url: '/admins', method: :post do |f| %>
    <div class="field">
      <%= f.label t('views.form.name') %><br/>
      <%= f.text_field :name %>
    </div>

    <div class="field">
      <%= f.label t('views.form.post') %><br/>
      <%= f.text_field :post %>
    </div>

    <div class="field">
      <%= f.label t('views.form.phone') %><br/>
      <%= f.number_field :phone %>
    </div>

    <div class="field">
      <%= f.label t('views.form.avatar') %>
      <%= f.file_field :avatar %>
    </div>
    <div class="field">
      <%= f.label t('views.form.email') %>
      <%= f.text_field :email %>
    </div>
    <div class="field">
      <%= f.label t('views.form.password') %>
      <%= f.text_field :password %>
    </div>
    <p>
      <%= f.submit t('views.admins.new.title') %>
    </p>
<% end %>
