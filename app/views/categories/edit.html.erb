<h2>Редагувати</h2>
<%= form_for :category, url: category_path(@category), method: :put do |f| %>
    <div class="field">
      <%= f.label 'Імя' %><br/>
      <%= f.text_field :name %>
    </div>
    <p>
      <%= f.submit 'Редагувати' %>
    </p>
<% end %>
