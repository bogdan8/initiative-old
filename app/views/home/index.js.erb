$("<%= escape_javascript render('home/index') %>").insertBefore('#show-initiatives-more');
<% if @last_page %>
$('.next-page-pagination').remove();
<% else %>
var href = '<%= root_path %>' + '?page=' + '<%= @current_page + 1 %>';
$('.next-page-pagination').attr('href', href);
<% end %>